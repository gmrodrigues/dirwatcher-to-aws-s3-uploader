Watch:
  Basepath:
    - /usr/bin
  Discover:
    RefreshMillis: 30000
    CmdLines:
      - "find /tmp -maxdepth 1 -type d"
  Filter:
    RegexWhiteList:
      - "^/root/[^/]+"
      - "^(/[^/]+){1,5}"